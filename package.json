{
  "name": "lbs-to-kg",
  "version": "1.0.0",
  "description": "",
  "main": "src/calculator.ts",
  "scripts": {
    "test-tsc": "tsc --version",
    "start": "npm run compile && node lib/calculator.js",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.1",
    "typescript": "^4.0.3"
  }
}
